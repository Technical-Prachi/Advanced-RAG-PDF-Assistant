services:
  valkey:
    image: valkey/valkey
    ports:
      - "6379:6379"

  mongodb:
    image: mongo:7
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: